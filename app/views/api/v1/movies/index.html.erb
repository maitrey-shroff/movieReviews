<div class="container" id="app">
<!--   <h1>ReVue It!</h1>
  <div v-for="movie in movies">
    <img :src="movie.poster"/>
    <h2>{{ movie.title }}</h2>
    <h5>Release date: {{ movie.release_date}}</h5>
    <p><b>Overview: </b>{{ movie.overview}}</p>
    </br>
  </div> -->

  <div v-for="movie in movies">

    <div class="col-xs-12 col-md-6 col-lg-6">
      <div class="jumbotron">
        <div class="center_everything">
          <h2>{{ movie.title }}</h2>
          <img :src="movie.poster"/>
          <h5>Release Date: {{ movie.release_date }}</h5>
          <div class="panel panel-default">
            <div class="panel-heading">
              <p class="panel-title"><b>Overview</b></p>
            </div>
            <div class="panel-body">
              {{ movie.overview }}
            </div>
          </div>
          <a class="btn btn-primary btn-md" :href="movie.review_path" role="button">ReVue movie</a>
          <a class="btn btn-primary btn-md" :href="movie.movie_reviews_path" role="button">ReVues for this movie</a>
        </div>
      </div> <!-- jtron -->
    </div> <!-- col -->
  </div> <!-- loop -->



</div>
